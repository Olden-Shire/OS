/.gradle/
/.idea/

build/
*.class
*.java~

hs_err_pid*.log

.DS_Store

# Reference implementations (not part of this repo's history)
/Engine-TS/
/Content-old/

# Content/ IS tracked: it carries the curated names/renames that build the cache
# (it stopped being derive-only once we started renaming). Repack scratch isn't.
/cache_repacked/

# Rust
/target/
/target_audit/
**/*.rs.bk
Cargo.lock.bak


# Reference trees (local-only)
/Engine2007/
/RuneScriptTS-main/
/reference/

# Derived cache unpack output
/unpacked/

# Compiled RuneScript bundle (regenerated from Content\scripts by the server on
# boot when JDK 21 is present; see compile_server_scripts / RUN.md).
/data/pack/

# Gradle state anywhere (runescript/)
.gradle/

# wasm-bindgen output (rebuilt from source; see crates/client/web/index.html)
crates/client/web/pkg/

# Root-level dev scratch: run logs and screenshots (the .ps1 launcher scripts in
# root ARE tracked - CI and end users run them).
/*.log
/*.png
/*.jpg
/sanity_stacks.txt
/cdp_targets.json
/devnull.txt
/audit_progress.md

# IntelliJ Platform Gradle plugin build state
/runescript/.intellijPlatform/

/.gradle/
/.idea/

build/
*.class
*.java~

hs_err_pid*.log

.DS_Store

# Reference implementations (not part of this repo's history)
/Engine-TS/
/Content-old/

# Derived from /cache via `cargo run -p app -- unpack` / `pack` — large (~400MB)
/Content/
/cache_repacked/

# Rust
/target/
/target_audit/
**/*.rs.bk
Cargo.lock.bak


# Reference trees (local-only)
/Engine2007/
/RuneScriptTS-main/

# Derived cache unpack output
/unpacked/

# Gradle state anywhere (runescript/)
.gradle/

# wasm-bindgen output (rebuilt from source; see crates/client/web/index.html)
crates/client/web/pkg/

# Root-level dev scratch: run logs, screenshots, one-off scripts
/*.log
/*.png
/*.jpg
/*.ps1
/sanity_stacks.txt
/cdp_targets.json
/devnull.txt
/audit_progress.md

# IntelliJ Platform Gradle plugin build state
/runescript/.intellijPlatform/

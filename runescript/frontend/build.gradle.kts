// Shared compiler frontend: lexer, parser, AST, symbol tables, and the
// pack-backed metadata loader. No dependencies beyond the Kotlin stdlib so
// it stays trivially embeddable in the IntelliJ plugin.
dependencies {
}
